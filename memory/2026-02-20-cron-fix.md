# 🔧 定时任务修复方案

## ⚠️ 权限问题

修改系统定时任务需要**管理员权限**。

---

## 🚨 问题诊断结果

| 问题 | 当前状态 |
|------|----------|
| 触发器类型 | ❌ 仅登录时触发 (MSFT_TaskLogonTrigger) |
| 定时间隔 | ❌ 无 |
| jobs.json | ❌ 空的 `{"jobs": []}` |
| NextRunTime | ❌ 空 |

---

## ✅ 解决方案

### 方案 1: 手动修复 (需要管理员权限)

**步骤 1**: 以**管理员身份**打开 PowerShell

**步骤 2**: 运行以下命令

```powershell
# 删除旧任务
Unregister-ScheduledTask -TaskName "openclaw" -Confirm:$false

# 创建新任务 (每10分钟运行)
$trigger = New-ScheduledTaskTrigger -Once -At (Get-Date) -RepetitionInterval (New-TimeSpan -Minutes 10)
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-WindowStyle Hidden -ExecutionPolicy Bypass -File `"C:\openclaw\start-openclaw.ps1`""
$settings = New-ScheduledTaskSettingsSet -StartWhenAvailable -DontStopOnIdleEnd

Register-ScheduledTask -TaskName "openclaw" -Trigger $trigger -Action $action -Settings $settings -RunLevel Highest -User "$env:USERNAME"
```

---

### 方案 2: 使用任务计划程序 GUI

1. Win+R → 输入 `taskschd.msc`
2. 找到 "openclaw" 任务
3. 右键 → **属性**
4. **触发器** 选项卡 → **新建**
5. 选择 **按计划** → **每天**
6. **高级设置** → 勾选 **重复任务间隔** → **10 分钟**
7. 确定保存

---

### 方案 3: 更新 jobs.json

编辑 `C:\Users\1990h\.openclaw\cron\jobs.json`:

```json
{
  "version": 1,
  "jobs": [
    {
      "id": "status-report",
      "name": "状态汇报",
      "schedule": "*/10 * * * *",
      "action": "report",
      "enabled": true
    }
  ]
}
```

---

## 📋 推荐操作

1. **立即**: 使用**方案 2** (GUI 方式，最简单)
2. **验证**: 检查 NextRunTime 是否有值
3. **测试**: 等待 10 分钟确认任务执行

---

## ⏰ 下次汇报

**如果定时任务修复成功**:
- 14:25 (10分钟后)
- 14:35 (20分钟后)
- 每10分钟自动汇报

**如果定时任务未修复**:
- 我会在您下次询问时汇报

---

**诊断完成**: 2026-02-20 14:17
**需要管理员权限**: 是
